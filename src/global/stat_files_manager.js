const platform = getItem("platform")

// search for file with the platform name
// if not found return "no stats file found"

// read file, if empty displa "file empty"
// if not empty take content, reformat into a usable array and extract it

